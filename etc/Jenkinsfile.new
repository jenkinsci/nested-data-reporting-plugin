pipeline {
	agent any
	
	stages {
		stage('Publish Data') {
			steps {
				checkout scm
				publishReports failOnError: true, displayType: "dual", provider: json(pattern: "etc/*.json")
			} 
		}
	}
}
